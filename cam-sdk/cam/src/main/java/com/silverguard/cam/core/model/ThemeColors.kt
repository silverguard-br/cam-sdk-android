package com.silverguard.cam.core.model

import android.graphics.Color

data class ThemeColors(
    val primary: Int,
    val primary04: Int?,
    val background: Color? = Colo,
    val label: Int?,
    val surface: Int?,
)
